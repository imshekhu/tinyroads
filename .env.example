# Persistent Socket.io backend used by online race mode.
VITE_MULTIPLAYER_URL=https://your-tinyroads-server.example.com

# Server-side comma-separated browser origins.
CLIENT_URLS=https://imshekhu.github.io

# Multiplayer server port (hosting providers generally inject this).
PORT=3001
